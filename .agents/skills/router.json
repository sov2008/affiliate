{
  "skills": {
    "fs_workspace_manager": {
      "description": "Safe read, patch, and file creation for Astro pages, components, SQLite models, and blog markdown posts.",
      "command": "node -e 'console.log(\"fs_workspace_manager ready\")'"
    },
    "bash_build_runner": {
      "description": "Autonomous build and validation runner for Astro static blog and TypeScript core.",
      "command": "npm run build:blog"
    },
    "sqlite_queue_operator": {
      "description": "Transactional SQLite content queue manager for BLOG_POST and SOCIAL_SNIPPET lifecycle.",
      "command": "npx tsx core/src/db/queueRepository.ts"
    },
    "ai_gateway_invoker": {
      "description": "Dating SEO longread and social snippet AI content generator with Frontmatter validation.",
      "command": "npx tsx core/src/services/blogGenerator.service.ts"
    },
    "remote_ssh_deployer": {
      "description": "Production sync and PM2 process deployment to DigitalOcean Droplet 178.128.199.28.",
      "command": "npx tsx core/src/scripts/deployDashboardUpdate.ts"
    },
    "http_verifier": {
      "description": "Automated verification of live HTTP status codes for flirtcheck.site blog and dashboard.",
      "command": "npx tsx core/src/scripts/verifyEndpoints.ts"
    },
    "skill_campaign_builder": {
      "description": "Generates a complete campaign landing page for an offer using auto-builder.",
      "command": "tsx src/auto-builder.ts"
    },
    "skill_tracking_validator": {
      "description": "Validates URL macros (sub1, click_id, sub_id) and CTA redirects in the campaigns directory.",
      "command": "tsx src/skills/tracking-validator.ts"
    },
    "skill_cloudflare_sync": {
      "description": "Verifies build output and triggers cloudflare deployment.",
      "command": "tsx src/skills/cloudflare-sync.ts"
    }
  }
}
